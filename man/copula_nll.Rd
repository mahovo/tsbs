% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CGARCH_PACKAGE_DOCUMENTATION.R, R/cgarch.R
\name{copula_nll}
\alias{copula_nll}
\title{Copula Negative Log-Likelihood for DCC(1,1)}
\usage{
copula_nll(
  params,
  z_matrix,
  weights,
  Qbar,
  copula_dist = "mvn",
  use_reparam = TRUE
)
}
\arguments{
\item{params}{Parameter vector (psi, phi) or (psi, phi, shape)}

\item{z_matrix}{Matrix of copula residuals (T x k)}

\item{weights}{Observation weights}

\item{Qbar}{Unconditional covariance matrix}

\item{copula_dist}{Copula distribution ("mvn" or "mvt")}

\item{use_reparam}{Logical; if TRUE, params are in reparameterized space}
}
\value{
Scalar negative log-likelihood

Scalar negative log-likelihood
}
\description{
Computes the weighted copula negative log-likelihood for DCC(1,1) models.

Computes the weighted copula negative log-likelihood
}
\details{
\subsection{Gaussian Copula (MVN)}{

The copula log-likelihood contribution at time t is:
ℓ_t = -1/2 (log|R_t| + z_t' R_t^{-1} z_t - z_t' z_t)

where R_t is the dynamic correlation matrix from DCC recursion.
}

\subsection{Student-t Copula (MVT)}{

ℓ_t = log Γ((k+ν)/2) - log Γ(ν/2) - (k/2) log(π(ν-2))
- (1/2) log|R_t|
- ((ν+k)/2) log(1 + q_t/(ν-2))
- Σ_i log f_t(z_{i,t}; ν)

where q_t = z_t' R_t^{-1} z_t is the Mahalanobis distance and f_t is the
univariate Student-t density.
}

\subsection{Reparameterization}{

When \code{use_reparam = TRUE}, the function expects parameters in the
unconstrained (ψ, φ) space rather than (α, β).
}
}
\seealso{
\code{\link[=copula_gradient]{copula_gradient()}} for the gradient
}
\keyword{internal}
