% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap}
\alias{bootstrap}
\title{General Bootstrap Function for Time Series with Flexible Block Structures}
\usage{
bootstrap(
  x,
  n_length = 100,
  block_length = 10,
  block_type = "stationary",
  func = mean,
  apply_func_to = "cols",
  num_blocks = 10,
  num_boots = 100,
  p_method = "1/n",
  p = NULL,
  overlap = TRUE,
  ar_order = 1,
  nstates = 2,
  model_func = NULL,
  score_func = NULL
)
}
\arguments{
\item{x}{A numeric vector, matrix, or data frame of time series data.}

\item{n_length}{Desired length of the bootstrap series. If \code{num_blocks} is given, \code{n_length} is overridden.}

\item{block_length}{Length of individual blocks (ignored for MSAR and HMM types).}

\item{block_type}{Type of block sampling: "moving", "stationary", "hmm", "msar", or "wild".}

\item{func}{A function to apply to each bootstrap sample (e.g. mean, standard deviation).}

\item{apply_func_to}{Whether to apply function to each column ("cols") or full sample ("df").}

\item{num_blocks}{Number of blocks per bootstrap sample.}

\item{num_boots}{Number of bootstrap replicates.}

\item{p_method}{Method to estimate stationary bootstrap p: "1/n", "plugin", or "cross validation".}

\item{p}{Optional value for p. If \code{NULL}, uses \code{p_method}.}

\item{overlap}{Allow overlapping blocks (for moving/stationary block types).}

\item{ar_order}{AR order for MSAR model.}

\item{nstates}{Number of regimes for HMM/MSAR models.}

\item{model_func}{Model function used when \code{p_method="cross validation"}.}

\item{score_func}{Scoring function used when \code{p_method="cross validation"}.}
}
\value{
A list containing:
\describe{
\item{bootstrap_series}{List of bootstrapped time series.}
\item{func_outs}{List of outputs of \code{func} applied to each sample.}
\item{func_out_means}{Mean output across bootstrap samples.}
}
}
\description{
Performs time-series-aware block bootstrap using moving, stationary, hidden Markov model (HMM),
Markov-switching autoregressive (MSAR), or wild bootstrap. Includes automatic estimation of block parameters.
}
\examples{
set.seed(42)
x <- arima.sim(n = 100, list(ar = 0.8))
result <- bootstrap(
  x = x,
  block_length = 10,
  block_type = "stationary",
  num_blocks = 5,
  num_boots = 100,
  func = mean,
  apply_func_to = "cols",
  p_method = "plugin"
)
print(result$func_out_means)

}
