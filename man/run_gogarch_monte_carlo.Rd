% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ms-varma-garch_diagnostics.R
\name{run_gogarch_monte_carlo}
\alias{run_gogarch_monte_carlo}
\title{Run Monte Carlo Study for GOGARCH Estimation}
\usage{
run_gogarch_monte_carlo(
  n_sim = 100,
  n_obs = 500,
  k = 3,
  true_omega = NULL,
  true_alpha = NULL,
  true_beta = NULL,
  omega = NULL,
  alpha_garch = NULL,
  beta_garch = NULL,
  distribution = "norm",
  shape = 8,
  ica_method = "radical",
  confidence_level = 0.95,
  verbose = TRUE,
  seed = 12345
)
}
\arguments{
\item{n_sim}{Number of simulation replications}

\item{n_obs}{Number of observations per replication}

\item{k}{Number of series/components (default 3)}

\item{true_omega}{Vector of true component GARCH omega parameters (alias for omega)}

\item{true_alpha}{Vector of true component GARCH alpha parameters (alias for alpha_garch)}

\item{true_beta}{Vector of true component GARCH beta parameters (alias for beta_garch)}

\item{omega}{Vector of component GARCH omega parameters (alternative to true_omega)}

\item{alpha_garch}{Vector of component GARCH alpha parameters (alternative to true_alpha)}

\item{beta_garch}{Vector of component GARCH beta parameters (alternative to true_beta)}

\item{distribution}{Component distribution ("norm" or "std")}

\item{shape}{Degrees of freedom for "std" distribution}

\item{ica_method}{ICA algorithm ("radical" or "fastica")}

\item{confidence_level}{Confidence level for coverage (default 0.95)}

\item{verbose}{Print progress}

\item{seed}{Base seed for reproducibility}
}
\value{
List with:
\item{estimates}{List of data frames (one per component) with alpha, beta columns}
\item{persistence}{List of vectors (one per component)}
\item{convergence}{Logical vector indicating optimization convergence}
\item{ica_converged}{Logical vector indicating ICA convergence}
\item{bias}{List of named vectors (one per component)}
\item{rmse}{List of named vectors (one per component)}
\item{empirical_sd}{List of named vectors (one per component)}
\item{coverage}{List of named vectors (one per component)}
\item{mixing_recovery}{Statistics on mixing matrix recovery}
\item{summary}{Summary data frame}
}
\description{
Performs a Monte Carlo simulation study to assess the accuracy
of GOGARCH parameter estimation. Since GOGARCH estimates component-wise
GARCH parameters, this evaluates estimation accuracy for each component.
}
