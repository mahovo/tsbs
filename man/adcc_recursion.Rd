% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cgarch.R
\name{adcc_recursion}
\alias{adcc_recursion}
\title{ADCC(1,1) Recursion}
\usage{
adcc_recursion(std_resid, Qbar, alpha, gamma, beta, Nbar = NULL)
}
\arguments{
\item{std_resid}{Matrix of standardized residuals (T x k)}

\item{Qbar}{Unconditional covariance matrix (k x k)}

\item{alpha}{ADCC alpha parameter (response to shocks)}

\item{gamma}{ADCC gamma parameter (asymmetric response to negative shocks)}

\item{beta}{ADCC beta parameter (persistence)}

\item{Nbar}{Unconditional covariance of negative shocks (k x k), computed if NULL}
}
\value{
List with success, Q, R, Nbar, and error info if failed
}
\description{
Computes Q and R matrices for ADCC(1,1) model.
ADCC adds asymmetric response to negative shocks via gamma parameter.

The ADCC recursion is:
Q_t = Ω + α*(z_{t-1}z'\emph{{t-1}) + γ*(n}{t-1}n'\emph{{t-1}) + β*Q}{t-1}

where n_t = z_t * I(z_t < 0) (element-wise negative indicator)
and Ω = (1 - α - β)\emph{Qbar - γ}Nbar
}
\keyword{internal}
