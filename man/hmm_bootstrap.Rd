% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmm_bootstrap.R
\name{hmm_bootstrap}
\alias{hmm_bootstrap}
\title{Hidden Markov Model (HMM) Bootstrap for Time Series}
\usage{
hmm_bootstrap(
  returns,
  nstates = 2,
  num_blocks = 100,
  num_boots = 100,
  shuffle_blocks = TRUE
)
}
\arguments{
\item{nstates}{Number of hidden states in the HMM.}

\item{num_boots}{Number of bootstrap samples to generate.}

\item{x}{A numeric matrix or data frame with time series (rows: time points, columns: series).}
}
\value{
A list of bootstrap replicates, each a matrix of the same dimension as \code{x}.
}
\description{
Fits a hidden Markov model to the time series and generates bootstrap replicates.
}
\examples{
set.seed(123)
returns <- arima.sim(n = 200, list(ar = 0.5))
boot_samples <- hmm_bootstrap(returns, nstates = 2, num_blocks = 10, num_boots = 50)

}
