% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostic_utils.R
\name{simulate_cgarch}
\alias{simulate_cgarch}
\title{Simulate CGARCH Data with ADCC Support}
\usage{
simulate_cgarch(
  n,
  k = 2,
  omega = NULL,
  alpha_garch = NULL,
  beta_garch = NULL,
  alpha_dcc = 0.04,
  beta_dcc = 0.93,
  gamma_dcc = NULL,
  copula = "mvn",
  shape = 8,
  Qbar = NULL,
  seed = NULL
)
}
\arguments{
\item{n}{Integer number of observations}

\item{k}{Integer number of series (default: 2)}

\item{omega}{Numeric vector of GARCH omega parameters (length k)}

\item{alpha_garch}{Numeric vector of GARCH alpha parameters (length k)}

\item{beta_garch}{Numeric vector of GARCH beta parameters (length k)}

\item{alpha_dcc}{Numeric: DCC alpha parameter (default: 0.04)}

\item{beta_dcc}{Numeric: DCC beta parameter (default: 0.93)}

\item{gamma_dcc}{Numeric: ADCC gamma parameter for leverage (default: NULL for standard DCC)}

\item{copula}{Character: copula type ("mvn" or "mvt")}

\item{shape}{Numeric: degrees of freedom for MVT copula (default: 8)}

\item{Qbar}{Matrix: unconditional correlation matrix. If NULL, uses moderate correlation.}

\item{seed}{Integer random seed}
}
\value{
Matrix of simulated returns (n x k)
}
\description{
Simulate data from a Copula-GARCH process with DCC or ADCC dynamics.
}
\details{
When gamma_dcc is provided (non-NULL and non-zero), the function simulates from
an ADCC (Asymmetric DCC) process where negative shocks have a larger impact on
correlation dynamics:

Q_t = Omega + alpha * (z_{t-1} z'\emph{{t-1}) + gamma * (n}{t-1} n'\emph{{t-1}) + beta * Q}{t-1}

where n_t = z_t * I(z_t < 0) captures negative shocks only.
}
