% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dcc_gradient.R,
%   R/ms-varma-garch_helper_functions.R
\name{dcc_recursion}
\alias{dcc_recursion}
\title{General DCC(p,q) Recursion (No Gradient)}
\usage{
dcc_recursion(std_resid, Qbar, alphas, betas, verbose = FALSE)

dcc_recursion(std_resid, Qbar, alphas, betas, verbose = FALSE)
}
\arguments{
\item{std_resid}{Matrix of standardized residuals (T x k)}

\item{Qbar}{Unconditional covariance matrix of standardized residuals (k x k)}

\item{alphas}{Numeric vector of alpha parameters (length q)}

\item{betas}{Numeric vector of beta parameters (length p)}

\item{verbose}{Logical; if TRUE, print diagnostic messages}
}
\value{
List with success, Q, R, maxpq, and error info if failed

List with components:
\item{success}{Logical indicating if recursion completed without errors}
\item{Q}{Array of Q matrices (k x k x T)}
\item{R}{Array of correlation matrices (k x k x T)}
\item{maxpq}{Maximum of p and q, used for burn-in period}
\item{error_type}{Character describing error if success is FALSE}
\item{error_time}{Time index where error occurred}
}
\description{
Compute Q and R matrices for arbitrary DCC order.

Computes the Q and R matrices for arbitrary DCC(p,q) order:
Q_t = Qbar * (1 - sum(alpha) - sum(beta)) +
sum_{j=1}^{q} alpha_j * (z_{t-j} * z_{t-j}') +
sum_{j=1}^{p} beta_j * Q_{t-j}
R_t = diag(Q_t)^{-1/2} * Q_t * diag(Q_t)^{-1/2}
}
\keyword{internal}
