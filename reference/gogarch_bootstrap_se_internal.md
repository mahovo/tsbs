# Internal GOGARCH bootstrap for unified interface

Internal GOGARCH bootstrap for unified interface

## Usage

``` r
gogarch_bootstrap_se_internal(
  residuals,
  weights,
  mle_result,
  n_boot,
  method,
  distribution,
  verbose,
  ...
)
```
