# Simulate ADCC Residuals

Simulate residuals from an ADCC model for parametric bootstrap.

## Usage

``` r
simulate_adcc_residuals(
  n,
  k,
  alpha,
  gamma,
  beta,
  Qbar,
  Nbar,
  copula_dist = "mvn",
  shape = NULL
)
```
