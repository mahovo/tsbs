# Compare States

Compare parameter estimates across states to assess regime
differentiation.

## Usage

``` r
compare_states(diagnostics, states = NULL)
```

## Arguments

- diagnostics:

  An object of class `ms_diagnostics`

- states:

  Integer vector of states to compare (if NULL, compares all)

## Value

List with comparison metrics
