# Profile One Parameter (Generic)

Internal function to compute profile likelihood for one parameter.

## Usage

``` r
profile_one_param_generic(
  param_idx,
  param_name,
  mle_params,
  mle_nll,
  nll_threshold,
  nll_fn,
  n_points,
  model_type
)
```
