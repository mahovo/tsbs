# Simulate DCC Standardized Residuals

Simulate standardized residuals from a DCC(1,1) model.

## Usage

``` r
simulate_dcc_residuals(
  n,
  k,
  alpha,
  beta,
  Qbar,
  distribution = "mvn",
  shape = NULL
)
```
