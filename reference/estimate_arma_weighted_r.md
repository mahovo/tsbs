# Estimate Conditional Mean Parameters (R Helper)

Estimate Conditional Mean Parameters (R Helper)

## Usage

``` r
estimate_arma_weighted_r(y, weights, spec, model_type = "univariate")
```

## Arguments

- y:

  y

- weights:

  weights

- spec:

  spec

- model_type:

  model_type
