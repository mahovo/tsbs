# Summary Method for tsbs_benchmark

Summary Method for tsbs_benchmark

## Usage

``` r
summary.tsbs_benchmark(object, ...)
```

## Arguments

- object:

  A `tsbs_benchmark` object.

- ...:

  Additional arguments (unused).

## Value

The summary data frame (invisibly).
