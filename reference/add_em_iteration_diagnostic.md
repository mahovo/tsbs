# Add EM Iteration Diagnostics

Add EM Iteration Diagnostics

## Usage

``` r
add_em_iteration_diagnostic(
  diagnostics,
  iteration,
  log_lik_before,
  log_lik_after,
  ll_change,
  duration_sec,
  parameters,
  convergence_flag = FALSE
)
```
