# Univariate GARCH + Distribution Parameter Estimation

Univariate GARCH + Distribution Parameter Estimation

## Usage

``` r
estimate_garch_weighted_univariate(residuals, weights, spec, verbose)
```
