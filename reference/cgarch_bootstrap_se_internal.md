# Internal CGARCH bootstrap for unified interface

Internal CGARCH bootstrap for unified interface

## Usage

``` r
cgarch_bootstrap_se_internal(
  residuals,
  weights,
  mle_result,
  n_boot,
  method,
  distribution,
  verbose,
  ...
)
```
