# Generate Correct Parameter Names for tsmarch

Translates a nested parameter list into the flat named list that
tsmarch's parmatrix expects (e.g., "omega\[1\]").

## Usage

``` r
generate_tsmarch_parnames(pars)
```

## Arguments

- pars:

  A nested parameter list.

## Value

A flat named list.
