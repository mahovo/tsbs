# Process Bootstrap Results

Compute bootstrap statistics from replications.

## Usage

``` r
process_bootstrap_results(
  boot_estimates,
  mle_params,
  param_names,
  n_boot,
  verbose = TRUE
)
```
