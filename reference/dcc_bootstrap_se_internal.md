# Internal DCC bootstrap for unified interface

Internal DCC bootstrap for unified interface

## Usage

``` r
dcc_bootstrap_se_internal(
  residuals,
  weights,
  mle_result,
  n_boot,
  method,
  distribution,
  verbose,
  ...
)
```
