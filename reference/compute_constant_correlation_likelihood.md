# Compute Constant Correlation Weighted Likelihood

Compute Constant Correlation Weighted Likelihood

## Usage

``` r
compute_constant_correlation_likelihood(
  residuals,
  weights,
  garch_pars,
  dist_pars,
  spec
)
```

## Arguments

- residuals:

  residuals

- weights:

  weights

- garch_pars:

  garch_pars

- dist_pars:

  dist_pars

- spec:

  spec
