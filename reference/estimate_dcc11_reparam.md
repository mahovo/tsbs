# Estimate DCC(1,1) using reparameterized optimization

Estimate DCC(1,1) using reparameterized optimization

## Usage

``` r
estimate_dcc11_reparam(
  std_resid,
  weights,
  dcc_start_pars,
  Qbar,
  distribution = "mvn",
  dist_pars = NULL,
  verbose = FALSE
)
```
