# Count DCC Parameters in Model (Deprecated)

Legacy function - use count_correlation_parameters instead.

## Usage

``` r
count_dcc_parameters(model_fits)
```

## Arguments

- model_fits:

  List of state-specific model fits

## Value

Integer count of DCC parameters
